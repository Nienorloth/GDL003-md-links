desarrollar herramienta que permita validar los links de un documento md.

como sera la interfaz? en la terminal
sera para un archivo o un directorio?
debe ser util para un desarrollador (usuario final)
sera una biblioteca de funcionalidad que se pueda exportar

en npm se publicara mi md links, para que otro desarrollador lo pueda usar para validar sus links

empezar por tests
no css, no html, no hay interfaz grafica
asincronia
como leer un directorio? cuanto tiempo tomara leerlo? como diferenciar un md de otros?
funciones pequeñas que hagan una sola cosa

fire system: sistema de archivos: indica que es un archivo, que es un directorio, que tipo de archivo es, extension, node-js es un proceso

diagrama de flujo para escribir nuestra logica

arbol de decisiones para solucionar el problema

semver: version semantica -  cada historia de usuario agrega un valor, un numero mas arriba.
parsing: interpretar, detectar caracteres para saber que es (titulo, parrafo, etc).

3 semanas

para checar un console log en el archivo index.js:
node index.js
node index

que pasaria con archivo json?




VIDEO DE FUNCIONES FIRESHIP

function declaration vs expression
recursive functions

una libreria es un conjunto de funciones que hacen algo en especifico, react es libreria

framework son conjunto de margenes, patrones que se deben respetar al trabajar con el

Brenda:
backslash... ó + para agarrar todo (expresiones regulares)
usar asincronía

metodo .match
los parentesis ahi te separan a un segundo elemento del array: "tengo mucho calor".match(/tengo (mucho) calor)
mucho es el segundo elemento del array que resulta
node.js modules investigar

NODE

para usar match necesito la ultima version de node.js
npm init: iniciar un proyecto en directoria actual en terminal, se creará archivo package.json en la terminal

para usar el modulo que creare: require("./index.js") o mdlinks.js o nombre que use para el archivo final.

para trabajar con metodos asincronicos = callback

usar eslint?
usar markdown-it para encontrar links en el archivo md (viene liga a repo en el readme del proyecto)



